# Hartmann Heat Pump - Unified Switch Components
# Pompa Ciepła Hartmann - Ujednolicone Komponenty Switch

switch:
  - platform: modbus_controller
    modbus_controller_id: ${modbus_controller_id}
    name: "${unit_power_name}"
    id: unit_power
    register_type: coil
    address: 1  # Unit main on/off control