# Hartmann Heat Pump - Switch Components (Polish)
# Pompa Ciepła Hartmann - Komponenty Switch

switch:
  - platform: modbus_controller
    modbus_controller_id: ${modbus_controller_id}
    name: "Wł/Wył jednostki"
    id: unit_power
    register_type: coil
    address: 1  # Główne sterowanie wł/wył jednostki